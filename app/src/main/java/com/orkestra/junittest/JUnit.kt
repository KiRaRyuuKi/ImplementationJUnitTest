package com.orkestra.junittest

class JUnit {
    // Fungsi add menerima dua parameter bilangan bulat (first dan second) dan mengembalikan hasil penjumlahan keduanya.
    fun add(first: Int, second: Int): Int {
        return first + second
    }
}
